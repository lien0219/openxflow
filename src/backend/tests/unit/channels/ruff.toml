extend = "../../../../../pyproject.toml"

# Channel gateway tests intentionally use synthetic credentials, inline exception
# messages, and fixture callbacks. Keep the test suite on correctness-oriented
# checks while production channel code continues to use the repository's full
# Ruff rule set.
[lint]
select = ["E4", "E7", "E9", "F", "I"]
